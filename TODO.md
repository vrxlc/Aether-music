# TODO

- [ ] Update `components/playlist-section.tsx` hero section to center the album cover mosaic and title/metadata within the right container.
- [x] Update `components/media-player.tsx` desktop player layout to remove the extra spacing/gap and bring the like + play/pause + volume controls closer together.

- [ ] Run typecheck/lint/build (or at least `pnpm -s lint` / `pnpm -s typecheck`) to ensure no TS errors.

